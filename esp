local ESPEnabled = false -
local lplr = game.Players.LocalPlayer 

local function ShowESP()
	for _, player in ipairs(game.Players:GetPlayers()) do
		local esp = Instance.new("Highlight")
		esp.Parent = player.Character
		esp.Enabled = true
	end
end
local a
for i,v in pairs(game:GetService("Players"):GetPlayers()) do
	a = v
end

local function DisableESP()
	for i,v in pairs(a.Character:GetChildren()) do
		if v:IsA("Highlight") then
			v:Destroy()
		end
	end
end

game.UserInputService.InputBegan:Connect(function(kc)
	if kc.KeyCode == Enum.KeyCode.E then
		if ESPEnabled == true then
			DisableESP()
			ESPEnabled = false
		else
			ShowESP()
			ESPEnabled = true
		end
	end
end)

--[[
  /__
 /     __)
/     /         |
///     (O) (O)|
  |           ------, 
  |         __/ 
  | (      /   \  \
  |        /  ___ \
  |        \      / / 
|____/
___./\
 /     .       | \  |
 \    /\   .  |  \ |\
 |`\       /\ |   | \
Jimmy's esp was founded by Jimmy me ofc (imanewman) for my dear friend headlined if u use this and u remove
this line of text u get sued 💰💸😔 

THIS IS SERIOUS I HAVE NO LAWYERS  😔😔😔
]]
